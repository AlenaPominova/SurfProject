<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pominova.surfmemesapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name="com.pominova.surfmemesapp.activity.StartActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.pominova.surfmemesapp.activity.AuthActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name="com.pominova.surfmemesapp.activity.MainActivity" />
    </application>
</manifest>
